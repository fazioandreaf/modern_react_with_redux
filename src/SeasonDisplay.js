// import the React and RactDom libraries
import React from 'react';

// Create a react component
const SeasonDisplay = ({children}) => {
    return (
		<div className="ui card">

		</div>
    )
}

export default SeasonDisplay
